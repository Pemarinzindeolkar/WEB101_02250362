/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['10.2.32.110', 'localhost', '127.0.0.1'],
};

export default nextConfig;